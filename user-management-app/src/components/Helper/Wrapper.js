// This component is meant to be used as a general wrapping component to prevent unnecessary usage of nested divs as wrapping components
const Wrapper = (props) =>  {
    return props.children;
}

export default Wrapper;